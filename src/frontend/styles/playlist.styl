.playlist-outer
    .playlist-actions
        display flex
        justify-content flex-end
        padding 10px 20px
        & > * + *
            margin-left 10px
        @media (max-width mobile-breakpoint)
            justify-content center
    .playlist-items
        padding 20px